

namespace EF.Contrib.Resources.System
{

    internal static class Strings
    {
        internal static string ConstructorWithArguments(object p0)
        {
            return string.Format("The complex or structural type {0} don't have a default constructor", p0);
        }
    }
}
